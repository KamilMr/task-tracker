import React from 'react';
import {Text, Box} from 'ink';

const View = () => {
  return (
    <Box borderColor="blue" borderStyle={'round'} width={'100%'}>
      <Text bold>View:</Text>
    </Box>
  );
};

export default View;
