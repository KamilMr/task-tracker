import React from 'react';
import {Text, Box} from 'ink';

const Projects = () => {
  return (
    <Box borderColor="greenBright" borderStyle={'round'} height={20}>
      <Text bold>Projects:</Text>
    </Box>
  );
};

export default Projects;
